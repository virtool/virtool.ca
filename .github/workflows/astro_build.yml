name: build

on:
  pull_request:
    branches:
      - "main"

jobs:
  build:
    runs-on: ubuntu-22.04

    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Astro Build
        uses: withastro/action@v0


