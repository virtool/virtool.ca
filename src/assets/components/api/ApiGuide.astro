---
import { render } from "astro:content";

const { section, title } = Astro.props;

const { Content } = await render(section);
---

<section class="col-span-7 prose max-w-4xl">
  <div class="prose max-w-4xl">
    <Content />
  </div>
</section>
